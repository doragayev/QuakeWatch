apiVersion: v1
kind: Service
metadata:
  name: quakewatch-service
spec:
  type: NodePort
  selector:
    app: quakewatch
  ports:
    - protocol: TCP
      port: 5000          
      targetPort: 5000    
      nodePort: 30007     

